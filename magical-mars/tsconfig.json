{
  "extends": "astro/tsconfigs/strict",
  "compilerOptions": {
    "baseUrl": ".", // Define la base para las rutas relativas
    "paths": {
      "@types/*": ["src/types/*"] // Alias para acceder a los tipos
    }
  },
  "include": [".astro/types.d.ts", "**/*"],
  "exclude": ["dist"]
}
